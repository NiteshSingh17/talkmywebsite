# HTTPS tunnel origin used in scrape URLs for ChatGPT
VITE_API_PUBLIC_URL=https://YOUR-SUBDOMAIN.ngrok-free.dev

# LOCAL API WebSocket do NOT use ngrok here. Use the port from: PORT=4000 npm run start:dev
VITE_WS_URL=ws://localhost:4000/ws
